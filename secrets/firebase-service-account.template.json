{
  "note": "Copy your Firebase Admin SDK credentials JSON into this folder as firebase-adminsdk.json (ignored).",
  "project_id": "your-project-id",
  "client_email": "svc-account@your-project-id.iam.gserviceaccount.com",
  "instructions": "Visit Firebase console -> Project settings -> Service accounts -> Generate new private key"
}
